#include <iostream>
#include <fstream>
using namespace std;

int n, b, c, s;
int main()
{
    ifstream in(27 - 6a.txt);
    in >> n;
    int a[n];
    for (int i = 0; i < n; i++) in >> a[i];
    for (int i = 0; i < n; i++)
    {
        if (a[i] % 6 == 0)
        {
            
        }
    }
        

    in
}
